# Local dev: leave empty — Vite proxies /api → Express (vite.config.ts).
# Vercel / production: REQUIRED — full URL of your Express API, no trailing slash.
# Example: https://ash-quick-pay-api.onrender.com
VITE_API_URL=
